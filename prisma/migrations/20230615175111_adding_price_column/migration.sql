BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Guest] ADD [price] DECIMAL(18,2) NOT NULL CONSTRAINT [Guest_price_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
